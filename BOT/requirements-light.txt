# Lightweight Requirements - Optimized for Render Free Tier (512MB limit)
# Removes heavy PyTorch/Transformers to stay under memory limit

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
httpx==0.25.2
python-dotenv==1.0.0
gunicorn==21.2.0

# Lightweight ML/NLP (no heavy models)
fuzzywuzzy>=0.18.0
python-levenshtein>=0.23.0

# Basic data processing (lightweight versions)
numpy>=1.26.0
scipy>=1.8.0

# Optional: Add back if memory allows
# scikit-learn>=1.3.0  # Commented out to save memory
# transformers>=4.35.0  # Heavy - causes memory issues
# torch>=2.6.0  # Very heavy - causes memory issues